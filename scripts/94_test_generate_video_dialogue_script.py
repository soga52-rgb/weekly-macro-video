#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
94 Test Generate Video Dialogue Script - Pro Model, News-Driven v3

Purpose:
- Read weekly_forest_summary.json generated by Step 91.
- Use Gemini Pro to convert the analysis into a Tom / Miranda dialogue script.
- News explains the market; data validates the market.
- Do NOT generate audio.

Input:
- output/weekly/YYYY-MM-DD/weekly_forest_summary.json

Output:
- output/weekly/YYYY-MM-DD/video_dialogue_script.json
- output/weekly/YYYY-MM-DD/video_dialogue_script.md
"""

import argparse
import json
import os
import socket
import urllib.error
import urllib.parse
import urllib.request
from pathlib import Path
from typing import Any, Dict, List


ROOT_DIR = Path(__file__).resolve().parents[1]
OUTPUT_WEEKLY_DIR = ROOT_DIR / "output" / "weekly"
DEFAULT_DIALOGUE_MODEL = "gemini-3.5-pro"


SYSTEM_PROMPT = """
你是專業總經影片腳本編輯與機構式市場敘事編劇，負責把 weekly_forest_summary.json 轉成 Tom / Miranda 雙人對談稿。

角色設定：
- Tom：主持人，代表觀眾的思考節點。他負責鋪路、提出下一個觀察點、留下懸念，但不能提前說破答案或方向。
- Miranda：首席總經策略師。她已經看過完整報告，知道前因後果與下一段要講什麼。她負責用新聞事件、政策訊號與市場報導來解釋資產反應，並用清楚但專業的方式串起整條論證。

核心敘事規則：
1. 不要用數據解釋數據。數據只代表市場結果；新聞事件、政策訊號與市場報導才是解釋原因。
2. 每個重要判斷都要遵守：新聞事件 → 影響哪個預期 → 資產數據是否驗證 → 若不一致，找反向新聞或修正因子 → 再進入下一個框架。
3. 若單一新聞線對下一個變數的解釋力很強，Miranda 可以明確順推。
4. 若前後變數不同步，Miranda 必須指出直覺矛盾，並用反向新聞、政策訊號或市場結構因素解釋抵銷。
5. Miranda 的每段回答要有分析師洞察，不得只摘要資料；她要判斷哪條新聞線比較有市場解釋力。
6. Tom 的問題不能透露下一幕答案，例如不要問「為什麼利率這麼強」，而要問「本週 10 年期公債殖利率的走勢是不是很值得注意？」
7. Miranda 可以在每段結尾埋下一段伏筆，但不能提前展開下一段完整分析。
8. 影片圖是視覺錨點，不是講稿來源。Miranda 要把 web_visual_pages、presentation_pages、transmission_diagnosis、macro_storyline、evidence 裡較完整的分析轉成口語導讀。
9. 風格是機構式總經導讀，不是財經 YouTuber。可以自然口語，但避免「精神分裂」、「尚方寶劍」、「崩盤」、「全面失守」、「踢到鐵板」等過度戲劇化詞彙。
10. 嚴格輸出合法 JSON，不要 Markdown，不要多餘文字。
"""


USER_PROMPT_TEMPLATE = """
請根據以下 weekly_forest_summary.json 產生 video_dialogue_script.json。

任務：
- 產出 Tom / Miranda 雙人對談稿。
- 每個 scene 對應一組 scene_dialogues。
- 必須依照 video_visual_scenes 的順序。
- 每個 scene 的畫面主題要對齊 screen_title、single_message、on_screen_labels 與 visual_metaphor。
- 但旁白深度不能只來自 video_visual_scenes；必須優先使用 web_visual_pages、presentation_pages、transmission_diagnosis、macro_storyline、common_judgment_funnel、evidence 作為分析素材。
- Tom 負責提出下一個值得觀察的現象，不能提前揭露結論。
- Miranda 負責用新聞事件與政策訊號解釋市場定價，並預留下一段伏筆。

最重要的寫作邏輯：
1. 數據是結果，新聞是解釋。
2. 不要只說「WTI 跌、US10Y 升、DXY 震盪」；要說是哪一條新聞線或政策訊號讓市場這樣定價。
3. 單一因素若能解釋下一個變數，可以順勢推論。
4. 如果兩個變數不同步，必須找反向新聞或修正因子抵銷，不能硬推。
5. 找到抵銷因素後，才能跳到下一個框架。
6. 每一段 Miranda 都應該包含：表面現象 → 新聞原因 → 市場定價機制 → 保留判斷或下一段伏筆。
7. 每一段 Tom 都應該包含：觀眾自然會問的疑問或下一個觀察點，但不要把答案講出來。

每個 scene 的深度要求：
- scene_01 overview：講本週不是單一變數主導，而是多條新聞線重新排序市場風險。
- scene_02 inflation_expectation：用油價、CPI/PPI/PCE、就業/薪資與能源新聞來解釋通膨預期。可埋伏筆說通膨 mixed 會讓利率更敏感，但不要展開利率分析。
- scene_03 rate_expectation：用 Fed 會議紀錄、長債拋售、全球公債賣壓、期限溢價、美債需求等新聞線解釋殖利率，不要只說 US10Y 高。
- scene_04 dollar_index：用利差支撐與成長疑慮的新聞線解釋美元為何高檔震盪，不要只說 DXY 99。
- scene_05 asia_fx_gold：區分日圓、韓圜、台幣與黃金；用利差、美元流動性、央行態度、避險需求解釋，不要只說亞幣弱與黃金拉鋸。
- scene_06 next_week_roadmap：用 Fed 後續表態、美伊談判、全球長債賣壓、亞洲貨幣防線來整理下週觀察點。

輸出 JSON 結構：
{
  "meta": {
    "source": "weekly_forest_summary.json",
    "purpose": "Tom / Miranda news-driven dialogue script before TTS",
    "week_range": "",
    "scene_count": 0,
    "dialogue_model_note": ""
  },
  "title": "",
  "opening_hook": "",
  "speaker_profiles": {
    "Tom": {
      "display_name": "Tom",
      "role_label": "主持人",
      "voice_role": "male_host",
      "screen_position": "lower_left"
    },
    "Miranda": {
      "display_name": "Miranda",
      "role_label": "總經策略師",
      "voice_role": "female_strategist",
      "screen_position": "lower_left"
    }
  },
  "scene_dialogues": [
    {
      "scene_id": "scene_01",
      "scene_type": "",
      "screen_title": "",
      "single_message": "",
      "scene_goal": "",
      "source_material_used": [],
      "argument_step": {
        "surface_signal": "",
        "news_explanation": "",
        "market_pricing_logic": "",
        "offset_or_risk": "",
        "foreshadow_next": ""
      },
      "speaker_turns": [
        {
          "turn_id": "scene_01_turn_01",
          "speaker": "Tom",
          "display_name": "Tom",
          "role_label": "主持人",
          "spoken_text": "",
          "subtitle_text": "",
          "speaker_label_text": "Tom｜主持人",
          "estimated_seconds": 0,
          "news_reference": [],
          "visual_reference": ""
        },
        {
          "turn_id": "scene_01_turn_02",
          "speaker": "Miranda",
          "display_name": "Miranda",
          "role_label": "總經策略師",
          "spoken_text": "",
          "subtitle_text": "",
          "speaker_label_text": "Miranda｜總經策略師",
          "estimated_seconds": 0,
          "news_reference": [],
          "visual_reference": ""
        }
      ],
      "scene_summary": "",
      "avoid_saying": []
    }
  ],
  "tts_plan": {
    "voice_strategy": "Tom male host, Miranda female strategist",
    "needs_tts": true,
    "needs_speaker_label_overlay": true,
    "subtitle_mode": "turn_by_turn"
  },
  "quality_checks": {
    "scene_alignment_note": "",
    "news_driven_logic_note": "",
    "continuity_note": "",
    "risk_control_note": ""
  }
}

長度控制：
- 每個 scene 4~6 個 speaker_turns。
- Tom 每次 1~2 句，負責鋪路與提出懸念。
- Miranda 每次 2~4 句，必須有新聞脈絡與市場定價機制。
- 每個 scene 約 45~65 秒。
- 全片目標 5~7 分鐘。

語氣：
- 口語但不浮誇，像專業財經節目，不像逐字念報告。
- Tom 不能問出答案型問題；Tom 要問觀察型或懸念型問題。
- Miranda 不能只說變數上升或下降；Miranda 要說明是哪一條新聞線讓市場這樣反應。
- 可以用白話解釋專業詞，但不要過度比喻。
- 若提到 Fed 升息，請寫「市場重新納入升息風險」或「會議紀錄偏鷹」，不可寫成 Fed 已宣布升息。

weekly_forest_summary.json：
{weekly_forest_summary_json}
"""


def find_latest_week_dir() -> Path:
    week_dirs = [p for p in OUTPUT_WEEKLY_DIR.iterdir() if p.is_dir()]
    if not week_dirs:
        raise FileNotFoundError("No weekly output folder found under output/weekly/")
    week_dirs.sort(key=lambda p: p.name, reverse=True)
    return week_dirs[0]


def load_json(path: Path, default: Any = None) -> Any:
    if not path.exists():
        return default
    with path.open("r", encoding="utf-8-sig") as f:
        return json.load(f)


def save_json(path: Path, data: Dict[str, Any]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)


def save_text(path: Path, text: str) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(text, encoding="utf-8")


def compact_summary(summary: Dict[str, Any]) -> Dict[str, Any]:
    return {
        "meta": summary.get("meta", {}),
        "forest_summary": summary.get("forest_summary", {}),
        "common_judgment_funnel": summary.get("common_judgment_funnel", {}),
        "macro_storyline": summary.get("macro_storyline", {}),
        "macro_variables": summary.get("macro_variables", {}),
        "transmission_diagnosis": summary.get("transmission_diagnosis", {}),
        "web_visual_pages": summary.get("web_visual_pages", []),
        "presentation_pages": summary.get("presentation_pages", []),
        "overview_visual": summary.get("overview_visual", {}),
        "video_visual_scenes": summary.get("video_visual_scenes", []),
        "narration_outline": summary.get("narration_outline", []),
        "evidence": summary.get("evidence", {}),
        "video_planning": summary.get("video_planning", {}),
    }


def build_user_prompt(summary: Dict[str, Any]) -> str:
    return USER_PROMPT_TEMPLATE.replace(
        "{weekly_forest_summary_json}",
        json.dumps(compact_summary(summary), ensure_ascii=False, indent=2),
    )


def extract_json(text: str) -> Dict[str, Any]:
    text = text.strip()
    start = text.find("{")
    end = text.rfind("}")
    if start == -1 or end == -1 or end <= start:
        raise ValueError("Gemini response does not contain a JSON object.")
    return json.loads(text[start:end + 1], strict=False)


def call_gemini_json(system_prompt: str, user_prompt: str, model: str, api_key: str, temperature: float) -> Dict[str, Any]:
    endpoint = (
        "https://generativelanguage.googleapis.com/v1beta/models/"
        + urllib.parse.quote(model)
        + ":generateContent?key="
        + urllib.parse.quote(api_key)
    )
    payload = {
        "systemInstruction": {"parts": [{"text": system_prompt}]},
        "contents": [{"role": "user", "parts": [{"text": user_prompt}]}],
        "generationConfig": {
            "temperature": temperature,
            "topP": 0.9,
            "responseMimeType": "application/json",
        },
    }
    request = urllib.request.Request(
        endpoint,
        data=json.dumps(payload).encode("utf-8"),
        headers={"Content-Type": "application/json"},
        method="POST",
    )
    try:
        with urllib.request.urlopen(request, timeout=360) as response:
            raw = response.read().decode("utf-8")
    except urllib.error.HTTPError as exc:
        detail = exc.read().decode("utf-8", errors="replace")
        raise RuntimeError(f"Gemini dialogue HTTPError {exc.code}: {detail}") from exc
    except (urllib.error.URLError, TimeoutError, socket.timeout) as exc:
        raise RuntimeError(f"Gemini dialogue request failed or timed out: {exc}") from exc

    result = json.loads(raw)
    candidates = result.get("candidates") or []
    if not candidates:
        raise RuntimeError(f"Gemini returned no candidates. Preview: {raw[:1200]}")
    parts = (candidates[0].get("content") or {}).get("parts") or []
    text = "".join(str(part.get("text", "")) for part in parts)
    if not text.strip():
        raise RuntimeError(f"Gemini returned empty text. Preview: {raw[:1200]}")
    return extract_json(text)


def estimate_seconds(text: str) -> int:
    return max(3, min(22, round(len(text or "") / 5.2)))


def normalize_dialogue(data: Dict[str, Any], summary: Dict[str, Any]) -> Dict[str, Any]:
    scenes = summary.get("video_visual_scenes") or []
    scene_count = len(scenes) if isinstance(scenes, list) else 0
    data.setdefault("meta", {})
    data["meta"].setdefault("source", "weekly_forest_summary.json")
    data["meta"].setdefault("purpose", "Tom / Miranda news-driven dialogue script before TTS")
    data["meta"].setdefault("week_range", (summary.get("meta") or {}).get("week_range", ""))
    data["meta"].setdefault("scene_count", scene_count)

    data.setdefault("speaker_profiles", {
        "Tom": {"display_name": "Tom", "role_label": "主持人", "voice_role": "male_host", "screen_position": "lower_left"},
        "Miranda": {"display_name": "Miranda", "role_label": "總經策略師", "voice_role": "female_strategist", "screen_position": "lower_left"},
    })

    for scene in data.get("scene_dialogues", []) or []:
        turns = scene.get("speaker_turns") or []
        for idx, turn in enumerate(turns, start=1):
            speaker = str(turn.get("speaker") or "Miranda").strip()
            if speaker not in {"Tom", "Miranda"}:
                speaker = "Miranda"
            role = "主持人" if speaker == "Tom" else "總經策略師"
            turn["speaker"] = speaker
            turn["display_name"] = speaker
            turn["role_label"] = role
            turn["speaker_label_text"] = f"{speaker}｜{role}"
            turn.setdefault("turn_id", f"{scene.get('scene_id', 'scene')}_turn_{idx:02d}")
            spoken = str(turn.get("spoken_text", ""))
            turn.setdefault("subtitle_text", spoken[:60])
            turn["estimated_seconds"] = int(turn.get("estimated_seconds") or estimate_seconds(spoken))
    data.setdefault("tts_plan", {
        "voice_strategy": "Tom male host, Miranda female strategist",
        "needs_tts": True,
        "needs_speaker_label_overlay": True,
        "subtitle_mode": "turn_by_turn",
    })
    return data


def build_markdown(data: Dict[str, Any]) -> str:
    lines: List[str] = []
    lines.append(f"# 影片雙人旁白稿｜{data.get('title', '')}")
    lines.append("")
    if data.get("opening_hook"):
        lines.append("## Opening Hook")
        lines.append("")
        lines.append(str(data.get("opening_hook", "")))
        lines.append("")
    for scene in data.get("scene_dialogues", []) or []:
        lines.append(f"## {scene.get('scene_id', '')}｜{scene.get('screen_title', '')}")
        lines.append("")
        if scene.get("single_message"):
            lines.append(f"畫面訊息：{scene.get('single_message')}")
            lines.append("")
        if scene.get("argument_step"):
            lines.append("### 論證步驟")
            lines.append("")
            for key, value in (scene.get("argument_step") or {}).items():
                lines.append(f"- {key}: {value}")
            lines.append("")
        for turn in scene.get("speaker_turns", []) or []:
            speaker = turn.get("speaker", "")
            role = turn.get("role_label", "")
            spoken = turn.get("spoken_text", "")
            subtitle = turn.get("subtitle_text", "")
            lines.append(f"**[{speaker}｜{role}]** {spoken}")
            if subtitle:
                lines.append(f"> 字幕：{subtitle}")
            lines.append("")
        if scene.get("scene_summary"):
            lines.append(f"小結：{scene.get('scene_summary')}")
            lines.append("")
    return "\n".join(lines).strip() + "\n"


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--week-dir", type=str, default="")
    args = parser.parse_args()

    api_key = os.getenv("GEMINI_API_KEY", "").strip()
    if not api_key:
        raise EnvironmentError("Missing GEMINI_API_KEY.")

    model = os.getenv("GEMINI_DIALOGUE_MODEL", DEFAULT_DIALOGUE_MODEL).strip() or DEFAULT_DIALOGUE_MODEL
    try:
        temperature = float(os.getenv("DIALOGUE_TEMPERATURE", "0.6").strip())
    except ValueError:
        temperature = 0.6

    if args.week_dir:
        week_dir = Path(args.week_dir)
        if not week_dir.exists() and not week_dir.is_absolute():
            candidate = OUTPUT_WEEKLY_DIR / args.week_dir
            if candidate.exists():
                week_dir = candidate
    else:
        week_dir = find_latest_week_dir()

    summary_path = week_dir / "weekly_forest_summary.json"
    summary = load_json(summary_path, {})
    if not summary:
        raise FileNotFoundError(f"Missing or empty weekly_forest_summary.json: {summary_path}")

    print(f"[INFO] Week dir: {week_dir}")
    print(f"[INFO] Dialogue model: {model}")
    print(f"[INFO] Dialogue temperature: {temperature}")

    data = call_gemini_json(
        system_prompt=SYSTEM_PROMPT,
        user_prompt=build_user_prompt(summary),
        model=model,
        api_key=api_key,
        temperature=temperature,
    )
    data = normalize_dialogue(data, summary)

    json_path = week_dir / "video_dialogue_script.json"
    md_path = week_dir / "video_dialogue_script.md"
    save_json(json_path, data)
    save_text(md_path, build_markdown(data))

    print(f"[OK] Created {json_path}")
    print(f"[OK] Created {md_path}")


if __name__ == "__main__":
    main()
